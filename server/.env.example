PORT=8787
# Absolute path to the Firebase service account JSON. Do NOT commit the file
# this points to, and do not move it into this repo.
FIREBASE_SERVICE_ACCOUNT_PATH=/absolute/path/to/service-account.json
